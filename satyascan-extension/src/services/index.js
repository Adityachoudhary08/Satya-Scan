/**
 * services/index.js
 *
 * Service layer exports.
 *
 * Module 2 will add:
 *  - analyzeService.js   – POST /api/analyze   (full page analysis)
 *  - verifyService.js    – POST /api/verify     (selected text verification)
 *  - historyService.js   – Chrome storage reads/writes for recent scans
 *  - authService.js      – JWT token management
 *
 * All service functions will use the base URL from constants.js
 * and include the auth token from authService.
 */

// Placeholder — populated in Module 2
export {};
