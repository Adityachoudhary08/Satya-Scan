/**
 * utils/index.js
 *
 * Shared utility exports.
 *
 * Module 2 will add:
 *  - formatDate(timestamp)       – Human-readable relative time
 *  - truncateText(text, length)  – Safe text truncation
 *  - getRiskColor(score)         – Maps 0–100 risk score → CSS color token
 *  - getRiskLabel(score)         – Maps score → "Safe" | "Suspicious" | "Fake"
 *  - debounce(fn, delay)         – Debounce helper for API calls
 */

// Placeholder — populated in Module 2
export {};
